function init()
  effect.addStatModifierGroup({
    {stat = "grit", amount = 1},
    {stat = "protection", effectiveMultiplier = 1.5},
    {stat = "protection", amount = 5}
  })
end

function update(dt)
  
end

function uninit()

end
